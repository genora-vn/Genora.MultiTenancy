@page
@using Genora.MultiTenancy.Localization
@using Genora.MultiTenancy.Permissions
@using Genora.MultiTenancy.Web.Pages.AppCustomerTypes
@using Microsoft.AspNetCore.Authorization
@using Microsoft.Extensions.Localization
@inject IStringLocalizer<MultiTenancyResource> L
@inject IAuthorizationService AuthorizationService
@inject Volo.Abp.MultiTenancy.ICurrentTenant CurrentTenant

@{
    var canCreate = CurrentTenant.IsAvailable
        ? await AuthorizationService.IsGrantedAsync(MultiTenancyPermissions.AppCustomerTypes.Create)
        : await AuthorizationService.IsGrantedAsync(MultiTenancyPermissions.HostAppCustomerTypes.Create);
}


@section scripts
{
    <abp-script src="/Pages/AppCustomerTypes/Index.js" />
}

<abp-card>
    <abp-card-header>
        <abp-row>
            <abp-column size-md="_6">
                <abp-card-title>@L["AppCustomerTypes"]</abp-card-title>
            </abp-column>
            <abp-column size-md="_6" class="text-end">
                @* HOST: luôn thấy; TENANT: cần quyền *@
                @if (canCreate)
                {
                    <abp-button id="NewAppSettingButton"
                                text="@L["NewAppCustomerType"].Value"
                                icon="plus"
                                button-type="Primary" />
                }
            </abp-column>
        </abp-row>
    </abp-card-header>
    <abp-card-body>
        <abp-table striped-rows="true" id="CustomerTypesTable"></abp-table>
    </abp-card-body>
</abp-card>